name: CI
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-go@v5
      with:
        go-version-file: 'go.mod'
    - uses: actions/checkout@v4
    - run: go test -v -race ./...
